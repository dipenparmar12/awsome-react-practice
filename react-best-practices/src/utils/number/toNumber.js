/**
 * Convert a string to number
 * @param {String} str
 * @returns {Number} num
 * @src https://1loc.dev/number/convert-a-string-to-number/
 */
function toNumber(str) {
  return +str
}
export default toNumber
